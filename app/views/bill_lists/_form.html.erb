<%= form_for(@bill_list) do |f| %>
  <% if @bill_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bill_list.errors.count, "error") %> prohibited this bill_list from being saved:</h2>

      <ul>
      <% @bill_list.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
