<% provide(:title, 'Notes') %>
<h1> Note: <%= @note.id %> </h1>
<p> Body: <%= @note.body %> </p>
<p> Note Type: <%= @note.note_type %> </p>
<p> Note Type ID: <%= @note.note_type_id %> </p>
<p> User_id : <%= @note.user_id %> </p>

<%= link_to 'Edit', edit_note_path(@note) %> |
<%= link_to 'Back', notes_path %>
