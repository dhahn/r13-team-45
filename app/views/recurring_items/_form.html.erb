<%= form_for(@recurring_item) do |f| %>
  <% if @recurring_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@recurring_item.errors.count, "error") %> prohibited this recurring_item from being saved:</h2>

      <ul>
      <% @recurring_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
