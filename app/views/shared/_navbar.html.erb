<nav id="main-navbar" class="transparent cf">
  <% if current_user %>
    <div class="nav-item left"><span id="menu-button" aria-hidden="true" data-icon="&#xe602;"></span></div>
  <% end %>
  <span>Fridge</span>
  <% if current_user %>
    <div class="nav-item right">
      <% unless default_title_displayed? yield(:title) %>
        <%= link_to derive_path_to_new yield(:title) do %>
          <span id="add-button" aria-hidden="true" data-icon="&#xe601;"></span>
        <% end %>
      <% end %>
    </div>
  <% end %>
</nav>
