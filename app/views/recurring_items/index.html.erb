<h1>Listing recurring_items</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @recurring_items.each do |recurring_item| %>
  <tr>
    <td><%= link_to 'Show', recurring_item %></td>
    <td><%= link_to 'Edit', edit_recurring_item_path(recurring_item) %></td>
    <td><%= link_to 'Destroy', recurring_item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Recurring item', new_recurring_item_path %>
