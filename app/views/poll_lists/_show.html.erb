<div class="box polllist">
  <div class="inner-box">
    <% if can? :destroy, poll_list %>
      <%= link_to poll_list, :method => :delete , :class => "delete" do %>
        <span id="add-button" class="x" aria-hidden="true" data-icon="&#xe603;"></span>
      <% end %>
    <% end %>
    <h1><%= poll_list.question %></h1>
    <% poll_list.poll_list_items.each do |item| %>
      <div class="item cf" data-poll-list-item-id=<%= item.id %> data-user-id=<%= current_user.id %>>
        <span class="body"><%= "#{item.body}".html_safe %></span>
        <span class="value"><%= "#{item.value}" %></span>
        <span class="increment-poll" ><span aria-hidden="true" data-icon="&#xe601;"></span></span>
      </div>
    <% end %>
  </div>
  <%= link_to poll_list, :class => "blue-box" do %>
    <span id="menu-button" aria-hidden="true" data-icon="&#xe600;"></span> <%= poll_list.notes.length %> Comments
  <% end %>
</div>
